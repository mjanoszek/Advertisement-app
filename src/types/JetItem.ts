export interface JetItem {
  location: string;
  jetID: number;
  title: string;
  image: string;
  price: string;
  maxSpeed: string;
  range: string;
  engine: string;
  description: string;
  images: string[];
}
